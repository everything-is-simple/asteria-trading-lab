# 2026-06-30 Daily Status

## 今日第一轮完成

- 加固 `add_on / price_limit` core 4 research bundle 的 snapshot stub 契约：
  - `intended_front_filter_rule_id=Q-PRESSURE-ADJUST`
  - `intended_malf_background=pullback`
  - `manual_malf_fill_required_fields` 明确要求后续人工补：
    - `snapshot_quality_status=ready`
    - `malf_background=pullback`
    - `wave_range_break_fields.pressure_adjustment=true`
  - `research_boundary_warning` 明确声明：
    - stub 不是正式 front-filter ready
    - 必须先人工复核 MALF 结构证据
    - 不得从 research prep 生成交易
- 在真实数据根 materialize core 4 research-only bundle：
  - `data_root=Z:\asteria-trading-labs-data`
  - `research_bundle_root=research/add_on-price-limit-shortlist-v0.1`
  - `core_sample_count=4`
  - `backup_sample_count=2`
  - `core_daily_window_count=4`
  - `core_snapshot_stub_count=4`
- 复核真实输出：
  - core 4：`603538.SH / 603008.SH / 600310.SH / 603687.SH`
  - backup 2：`002663.SZ / 000899.SZ`
  - backup 样本没有被误写成 snapshot stub
  - core stub 仍保持 `snapshot_quality_status=source_missing`

## 今日第二轮完成

- 在 core 4 research bundle 里新增 `malf-snapshot-drafts-v0.1/`：
  - `600310.SH-2026-03.json`
  - `603008.SH-2026-03.json`
  - `603538.SH-2026-03.json`
  - `603687.SH-2026-03.json`
- draft 只补最小研究字段：
  - `snapshot_quality_status=incomplete`
  - `research_prep_status=draft_pending_manual_evidence_review`
  - `malf_background=pullback`
  - `wave_range_break_fields.pressure_adjustment=true`
  - `draft_front_filter_expected_rule_id=Q-PRESSURE-ADJUST`
- draft 明确不抢跑：
  - `draft_is_not_formal_front_filter_ready`
  - `snapshot_quality_status_must_remain_incomplete_until_reviewed`
  - `do_not_generate_trade_from_research_draft`

## 真实样本验证

实际运行命令：

```powershell
$env:PYTHONPATH='src'
python - <<'PY'
from data_sources.tdx_local import materialize_default_add_on_price_limit_core_malf_research_bundle
report = materialize_default_add_on_price_limit_core_malf_research_bundle(
    data_root=r'Z:\asteria-trading-labs-data',
    tdx_root=r'Z:\new_tdx64',
    offline_root=r'Z:\tdx_offline_Data',
    duckdb_root=r'Z:\malf-data',
    generated_at='2026-06-30T00:30:00+08:00',
)
print(report)
PY
```

关键结果：

- `result=pass`
- `research_only=true`
- `core_sample_count=4`
- `backup_sample_count=2`
- `core_daily_window_count=4`
- `core_snapshot_stub_count=4`
- `core_snapshot_draft_count=4`
- `formal_data_write_allowed=false`
- `institution_rule_definition_allowed=false`
- `signal_generation_allowed=false`
- `backtest_execution_allowed=false`
- `next_action=action:fill_core_malf_snapshot_stubs`

对 4 个 draft 逐个运行 `tachibana_front_filter.run_front_filter(...)` 后，结果均为：

- `front_filter_result=blocked`
- `qualification_rule_id=null`
- `rule_match_reason=["blocked_by_malf_snapshot_not_ready"]`

这说明 draft 虽然已经携带 `pullback / pressure_adjustment=true` 的研究假设，但仍不会进入正式 `Q-PRESSURE-ADJUST` 复核。

## 行业标签时间对齐复核

本轮直接核对 `Z:\malf-data\market_meta.duckdb` 与 `Z:\tdx_offline_Data` 后确认：

- 6 个 shortlist 样本的行业关系事实全部从 `2026-04-23` 才开始生效。
- 6 个样本的本地离线日线目前都只到 `2026-04-03`。
- 当前没有可用的“行业标签已生效之后的一段后续日线窗口”。

因此，当前 formal front-filter 阻断仍然成立：

- `industry_membership_window_not_overlapping`

这不是 shortlist 选样错误，也不是 front-filter 行为异常，而是行业标签时间轴与行情窗口确实没有重叠。

随后又补了一次时间对齐诊断：

- `read_daily_bars(...)` 已支持读取 `Z:\new_tdx64\vipdoc\<market>\lday` 下的更新日线。
- `Z:\new_tdx64` 中 6 个 shortlist 样本的日线都已经更新到 `2026-06-26`。
- 6 个样本在 `2026-04-23` 行业标签生效之后都有后续日线。
- 但这不能反推 `2026-03-24` 至 `2026-04-03` 原事件窗口的行业标签有效。

新增诊断入口：

```powershell
$env:PYTHONPATH='src'
python - <<'PY'
from data_sources.tdx_local import audit_add_on_price_limit_shortlist_time_alignment
report = audit_add_on_price_limit_shortlist_time_alignment(
    tdx_root=r'Z:\new_tdx64',
    offline_root=r'Z:\tdx_offline_Data',
    duckdb_root=r'Z:\malf-data',
    generated_at='2026-06-30T01:00:00+08:00',
)
print(report)
PY
```

关键结果：

- `result=blocked`
- `sample_count=6`
- `original_window_blocked_count=6`
- `post_label_daily_available_count=6`
- `next_action=action:rescreen_post_industry_effective_window`

因此，行业时间对齐的正确下一步不是借当前行业标签回填老窗口，而是用 `2026-04-23` 之后的更新日线重新筛选一批时间对齐候选。

## 当前判断

今天已经把“接下来最紧要几步”的前两步推进到可复跑状态：

- core 4 research bundle 已经可复跑、可落盘、字段契约更明确。
- 最小 MALF snapshot 已有 research draft，但仍保持 `incomplete`，不抢跑成正式 ready。
- 行业标签时间对齐阻断已经用真实数据再次坐实，同时确认了 `2026-04-23` 之后可以另开重筛窗口。

当前下一步不应写 reviewed relation evidence JSON。正确顺序仍然是：

1. 人工复核 core 4 draft 的 MALF 结构证据，决定是否可以把个别 draft 升级为 ready。
2. 对 `2026-04-23` 之后的更新日线重筛 `add_on / price_limit` 候选，形成时间对齐的新候选面。
3. 两者都满足后，再进入正式 front-filter research prep。

## 今日第三轮完成

- 新增 post-label 重筛入口：
  - `rescreen_add_on_price_limit_post_industry_window(...)`
  - 默认全市场路径先走 `Z:\malf-data\market_base_day.duckdb` 快筛，避免逐只股票扫描 `Z:\new_tdx64\vipdoc` 导致超时。
  - 显式传入 `ts_codes` 时，可对指定样本用 `Z:\new_tdx64` 更新日线做窗口重筛。
- 新增测试覆盖：
  - 未指定股票池时使用 DuckDB 快速筛选。
  - 指定股票池时可用 TDX `vipdoc` 日线筛出行业时间对齐 research-only 候选。
- 输出边界保持 research-only：
  - `formal_front_filter_ready_count=0`
  - `formal_data_write_allowed=false`
  - `institution_rule_definition_allowed=false`
  - `signal_generation_allowed=false`
  - `backtest_execution_allowed=false`
  - snapshot stub 仍为 `snapshot_quality_status=source_missing`

真实全市场 DuckDB 快筛命令：

```powershell
$env:PYTHONPATH='src'
@'
from data_sources.tdx_local import rescreen_add_on_price_limit_post_industry_window
report = rescreen_add_on_price_limit_post_industry_window(
    tdx_root=r'Z:\new_tdx64',
    duckdb_root=r'Z:\malf-data',
    window_start='2026-04-23',
    window_end='2026-06-26',
    limit=20,
    generated_at='2026-06-30T02:30:00+08:00',
)
print(report)
'@ | python -
```

关键结果：

- `result=blocked`
- `source_daily_root=duckdb_market_base_day`
- `candidate_count=0`
- `next_action=action:expand_post_industry_rescreen_window`

原因不是行业标签缺失，而是当前 DuckDB `market_base_day.base_bar` 只覆盖到 `2026-04-23`，这个单日全市场快筛没有命中 `add_on / price_limit` 候选。

随后对原 shortlist 6 只股票使用 `Z:\new_tdx64` 后续日线重筛：

```powershell
$env:PYTHONPATH='src'
@'
from data_sources.tdx_local import (
    default_add_on_price_limit_shortlist_sample_entries,
    rescreen_add_on_price_limit_post_industry_window,
)
codes = [item['ts_code'] for item in default_add_on_price_limit_shortlist_sample_entries()]
report = rescreen_add_on_price_limit_post_industry_window(
    tdx_root=r'Z:\new_tdx64',
    duckdb_root=r'Z:\malf-data',
    window_start='2026-04-23',
    window_end='2026-06-26',
    ts_codes=codes,
    limit=20,
    generated_at='2026-06-30T02:45:00+08:00',
)
print(report)
'@ | python -
```

关键结果：

- `result=pass`
- `source_daily_root=tdx_root`
- `scanned_symbol_count=6`
- `time_aligned_symbol_count=6`
- `candidate_count=4`
- `formal_front_filter_ready_count=0`
- `next_action=action:review_intraday_price_limit_reopen`

时间对齐候选：

| rank | ts_code | trade_date | nearest_limit_side | nearest_limit_gap_pct | current_industry_name |
| --- | --- | --- | --- | --- | --- |
| 1 | `603538.SH` | `2026-05-11` | `down_limit_side` | `0.0` | `化学制药` |
| 2 | `600310.SH` | `2026-06-05` | `down_limit_side` | `0.02` | `水力发电` |
| 3 | `603687.SH` | `2026-04-24` | `down_limit_side` | `0.02` | `广告包装` |
| 4 | `000899.SZ` | `2026-06-18` | `down_limit_side` | `0.46` | `火力发电` |

这 4 个候选只说明“行业时间轴与行情窗口已对齐，且有 post-label price-limit 研究形态”，不说明 MALF 结构已经 ready，也不允许进入正式 front-filter。

## 今日第四轮完成

- 新增 post-label intraday reopen 复核入口：
  - `review_add_on_price_limit_post_label_intraday_reopen(...)`
  - 输入为 post-label rescreen report。
  - 复用 `read_intraday_range(...)` 的 `lc5` 分时读取结果。
  - 输出 `reopened_after_limit_touch / closed_at_limit_after_touch / near_limit_without_touch / blocked` 四类研究状态。
- 该入口仍然只读、research-only：
  - `formal_front_filter_ready_count=0`
  - `formal_data_write_allowed=false`
  - `institution_rule_definition_allowed=false`
  - `signal_generation_allowed=false`
  - `backtest_execution_allowed=false`

真实复核运行：

```powershell
$env:PYTHONPATH='src'
@'
from data_sources.tdx_local import (
    default_add_on_price_limit_shortlist_sample_entries,
    rescreen_add_on_price_limit_post_industry_window,
    review_add_on_price_limit_post_label_intraday_reopen,
)
codes = [item['ts_code'] for item in default_add_on_price_limit_shortlist_sample_entries()]
rescreen = rescreen_add_on_price_limit_post_industry_window(
    tdx_root=r'Z:\new_tdx64',
    duckdb_root=r'Z:\malf-data',
    window_start='2026-04-23',
    window_end='2026-06-26',
    ts_codes=codes,
    limit=20,
    generated_at='2026-06-30T02:45:00+08:00',
)
review = review_add_on_price_limit_post_label_intraday_reopen(
    tdx_root=r'Z:\new_tdx64',
    rescreen_report=rescreen,
    generated_at='2026-06-30T03:10:00+08:00',
)
print(review)
'@ | python -
```

关键结果：

- `result=blocked`
- `reviewed_candidate_count=4`
- `blocked_intraday_review_count=4`
- `reopened_after_limit_touch_count=0`
- `closed_at_limit_after_touch_count=0`
- `near_limit_without_touch_count=0`
- `next_action=action:source_intraday_price_limit_review`

4 个 post-label 候选都被同一个分时证据问题阻断：

| ts_code | trade_date | intraday_review_reason |
| --- | --- | --- |
| `603538.SH` | `2026-05-11` | `intraday_trade_date_missing` |
| `600310.SH` | `2026-06-05` | `intraday_trade_date_missing` |
| `603687.SH` | `2026-04-24` | `intraday_trade_date_missing` |
| `000899.SZ` | `2026-06-18` | `intraday_trade_date_missing` |

当前判断：

- 行业时间对齐这道硬门已经找到可继续研究的后续窗口候选。
- 但 post-label 候选还缺对应交易日的 `lc5` 分时证据，不能确认“触板后打开 / 近板未触 / 封死”。
- 因此仍不能把任何 post-label 候选升级为正式 MALF ready，也不能跑正式 front-filter。
- 下一步是补齐或定位 `Z:\new_tdx64` 对应日期的 intraday source；如果分时源确实缺失，则只能降级为日线级 research hold，不能写 reviewed relation evidence JSON。

## 今日第五轮修正

根据立花义正波段交易法的证据粒度重新校正边界：

- 立花法/MALF 结构资格主线不应强制要求分时数据。
- `lc5`/`lc1` 分时只作为 A 股涨跌停事件细分的可选增强证据，用于区分“触板后打开 / 近板未触 / 封死”。
- `intraday_trade_date_missing` 不再阻断 post-label 日线级 MALF 结构复核，只说明盘中触板打开细节未复核。
- post-label 主线下一步改为：
  - `action:review_daily_level_malf_structure`

同时复核最新 `Z:\new_tdx64` 下载状态：

- 6 个 shortlist 样本的 `vipdoc/*/lday/*.day` 已更新到 `2026-06-29`。
- `.lc5` 仍停在 `2026-04-23`。
- `.lc1` 文件虽在 `2026-06-30` 早上更新，但这 6 个样本实际可读分钟线只覆盖到 `2026-04-16` 或 `2026-04-22`，仍不覆盖 `2026-05/06` 的 post-label 候选日。

实现修正：

- `review_add_on_price_limit_post_label_intraday_reopen(...)` 的 review scope 调整为：
  - `daily_level_malf_structure_with_optional_intraday_enhancement`
- 当分时缺失时：
  - `intraday_limit_reopen_status=intraday_optional_evidence_missing`
  - `daily_level_malf_review_status=pending`
  - `next_action=action:review_daily_level_malf_structure`
  - `result=pass`
- 仍保持所有硬闸关闭：
  - `formal_front_filter_ready_count=0`
  - `formal_data_write_allowed=false`
  - `institution_rule_definition_allowed=false`
  - `signal_generation_allowed=false`
  - `backtest_execution_allowed=false`

## 今日第六轮完成

- 新增日线级 MALF 结构复核入口：
  - `review_add_on_price_limit_post_label_daily_malf_structure(...)`
  - 输入为 post-label rescreen 或 optional-intraday review report。
  - 不依赖 `lc5/lc1` 分时数据。
  - 输出 `daily_level_malf_review_status=pass / blocked / manual_review_required`。
  - 输出 `daily_level_malf_structure_hint=pullback_pressure_adjustment / structure_unclear / not_applicable`。
- 该入口仍然只读、research-only，不生成正式 front-filter ready。

真实数据复核运行：

```powershell
$env:PYTHONPATH='src'
@'
from data_sources.tdx_local import (
    default_add_on_price_limit_shortlist_sample_entries,
    rescreen_add_on_price_limit_post_industry_window,
    review_add_on_price_limit_post_label_intraday_reopen,
    review_add_on_price_limit_post_label_daily_malf_structure,
)
codes = [item['ts_code'] for item in default_add_on_price_limit_shortlist_sample_entries()]
rescreen = rescreen_add_on_price_limit_post_industry_window(
    tdx_root=r'Z:\new_tdx64',
    duckdb_root=r'Z:\malf-data',
    window_start='2026-04-23',
    window_end='2026-06-29',
    ts_codes=codes,
    limit=20,
    generated_at='2026-06-30T10:20:00+08:00',
)
intraday = review_add_on_price_limit_post_label_intraday_reopen(
    tdx_root=r'Z:\new_tdx64',
    rescreen_report=rescreen,
    generated_at='2026-06-30T10:25:00+08:00',
)
daily = review_add_on_price_limit_post_label_daily_malf_structure(
    intraday,
    generated_at='2026-06-30T10:30:00+08:00',
)
print(daily)
'@ | python -
```

关键结果：

- `result=pass`
- `reviewed_candidate_count=4`
- `daily_level_malf_review_pass_count=4`
- `daily_level_malf_manual_review_required_count=0`
- `daily_level_malf_blocked_count=0`
- `next_action=action:prepare_malf_snapshot_draft_review`

4 个通过日线级结构复核的候选：

| ts_code | trade_date | daily_level_malf_review_status | daily_level_malf_structure_hint |
| --- | --- | --- | --- |
| `603538.SH` | `2026-05-11` | `pass` | `pullback_pressure_adjustment` |
| `600310.SH` | `2026-06-05` | `pass` | `pullback_pressure_adjustment` |
| `603687.SH` | `2026-04-24` | `pass` | `pullback_pressure_adjustment` |
| `000899.SZ` | `2026-06-18` | `pass` | `pullback_pressure_adjustment` |

硬闸仍然关闭：

- `formal_front_filter_ready_count=0`
- `formal_data_write_allowed=false`
- `institution_rule_definition_allowed=false`
- `signal_generation_allowed=false`
- `backtest_execution_allowed=false`

当前下一步：

1. 对这 4 个 `pullback_pressure_adjustment` 候选准备 MALF snapshot draft review。
2. 仍需人工核对结构证据后，才允许把个别 draft 升级为正式 ready。
3. 不进入规则定义、信号、仓位或回测。

## 今日第七轮完成

- 新增 MALF snapshot draft review 准备入口：
  - `prepare_malf_snapshot_draft_review(...)`
  - 输入为日线级 MALF 结构复核 report。
  - 只对 `daily_level_malf_review_status=pass` 且存在 `snapshot_stub` 的候选生成 `suggested_snapshot_draft`。
  - draft 保持 `snapshot_quality_status=incomplete`，只进入人工复核，不升级 ready。
- 输出边界：
  - `snapshot_draft_review_status=ready_for_manual_review / hold`
  - `next_action=action:manual_review_malf_snapshot_drafts`
  - `formal_front_filter_ready_count=0`

真实数据串联运行：

```powershell
$env:PYTHONPATH='src'
@'
from data_sources.tdx_local import (
    default_add_on_price_limit_shortlist_sample_entries,
    prepare_malf_snapshot_draft_review,
    rescreen_add_on_price_limit_post_industry_window,
    review_add_on_price_limit_post_label_daily_malf_structure,
    review_add_on_price_limit_post_label_intraday_reopen,
)
codes = [item['ts_code'] for item in default_add_on_price_limit_shortlist_sample_entries()]
rescreen = rescreen_add_on_price_limit_post_industry_window(
    tdx_root=r'Z:\new_tdx64',
    duckdb_root=r'Z:\malf-data',
    window_start='2026-04-23',
    window_end='2026-06-29',
    ts_codes=codes,
    limit=20,
    generated_at='2026-06-30T11:00:00+08:00',
)
intraday = review_add_on_price_limit_post_label_intraday_reopen(
    tdx_root=r'Z:\new_tdx64',
    rescreen_report=rescreen,
    generated_at='2026-06-30T11:05:00+08:00',
)
daily = review_add_on_price_limit_post_label_daily_malf_structure(
    intraday,
    generated_at='2026-06-30T11:10:00+08:00',
)
draft = prepare_malf_snapshot_draft_review(
    daily,
    generated_at='2026-06-30T11:15:00+08:00',
)
print(draft)
'@ | python -
```

关键结果：

- `result=pass`
- `draft_review_candidate_count=4`
- `draft_review_ready_count=4`
- `draft_review_hold_count=0`
- `next_action=action:manual_review_malf_snapshot_drafts`

4 个进入人工 draft review 的候选：

| ts_code | trade_date | snapshot_draft_review_status | snapshot_quality_status | malf_background | pressure_adjustment |
| --- | --- | --- | --- | --- | --- |
| `603538.SH` | `2026-05-11` | `ready_for_manual_review` | `incomplete` | `pullback` | `true` |
| `600310.SH` | `2026-06-05` | `ready_for_manual_review` | `incomplete` | `pullback` | `true` |
| `603687.SH` | `2026-04-24` | `ready_for_manual_review` | `incomplete` | `pullback` | `true` |
| `000899.SZ` | `2026-06-18` | `ready_for_manual_review` | `incomplete` | `pullback` | `true` |

硬闸仍然关闭：

- `formal_front_filter_ready_count=0`
- `formal_data_write_allowed=false`
- `institution_rule_definition_allowed=false`
- `signal_generation_allowed=false`
- `backtest_execution_allowed=false`

当前下一步：

1. 人工核对这 4 个 suggested snapshot draft 的结构证据。
2. 只允许从 `incomplete` 升级到“人工复核通过的 ready 候选”，仍不直接生成交易。
3. 下一层若继续实现，应新增“manual review verdict”入口，而不是直接跑正式 front-filter。

## 今日第八轮完成

- 新增 MALF snapshot 人工复核 verdict 入口：
  - `apply_malf_snapshot_manual_review_verdicts(...)`
  - 输入为 `prepare_malf_snapshot_draft_review(...)` 输出和人工 verdict map。
  - 支持：
    - `approved_for_formal_front_filter_review`
    - `rejected`
    - `needs_revision`
  - approved 只生成 `reviewed_snapshot_candidate`，不直接进入正式 front-filter ready。
- 输出边界：
  - `manual_review_status=reviewed_ready_candidate / rejected / needs_revision / needs_manual_review`
  - `reviewed_snapshot_candidate.snapshot_quality_status=reviewed_ready_candidate`
  - `next_action=action:prepare_formal_front_filter_review_package`
  - `formal_front_filter_ready_count=0`

真实数据 smoke 运行中，模拟 4 个 suggested draft 均由人工批准：

```powershell
$env:PYTHONPATH='src'
@'
from data_sources.tdx_local import (
    apply_malf_snapshot_manual_review_verdicts,
    default_add_on_price_limit_shortlist_sample_entries,
    prepare_malf_snapshot_draft_review,
    rescreen_add_on_price_limit_post_industry_window,
    review_add_on_price_limit_post_label_daily_malf_structure,
    review_add_on_price_limit_post_label_intraday_reopen,
)
codes = [item['ts_code'] for item in default_add_on_price_limit_shortlist_sample_entries()]
rescreen = rescreen_add_on_price_limit_post_industry_window(
    tdx_root=r'Z:\new_tdx64',
    duckdb_root=r'Z:\malf-data',
    window_start='2026-04-23',
    window_end='2026-06-29',
    ts_codes=codes,
    limit=20,
    generated_at='2026-06-30T12:00:00+08:00',
)
intraday = review_add_on_price_limit_post_label_intraday_reopen(
    tdx_root=r'Z:\new_tdx64',
    rescreen_report=rescreen,
    generated_at='2026-06-30T12:05:00+08:00',
)
daily = review_add_on_price_limit_post_label_daily_malf_structure(
    intraday,
    generated_at='2026-06-30T12:10:00+08:00',
)
draft = prepare_malf_snapshot_draft_review(
    daily,
    generated_at='2026-06-30T12:15:00+08:00',
)
verdicts = {
    item['ts_code']: {
        'manual_review_verdict': 'approved_for_formal_front_filter_review',
        'reviewer_note': 'Manual structure evidence check accepted for formal review package.',
    }
    for item in draft['candidates']
}
manual = apply_malf_snapshot_manual_review_verdicts(
    draft,
    verdicts,
    generated_at='2026-06-30T12:20:00+08:00',
)
print(manual)
'@ | python -
```

关键结果：

- `result=pass`
- `manual_reviewed_candidate_count=4`
- `manual_review_approved_count=4`
- `manual_review_hold_count=0`
- `next_action=action:prepare_formal_front_filter_review_package`

4 个进入 formal review package 准备的候选：

| ts_code | manual_review_status | reviewed_snapshot_candidate.snapshot_quality_status |
| --- | --- | --- |
| `603538.SH` | `reviewed_ready_candidate` | `reviewed_ready_candidate` |
| `600310.SH` | `reviewed_ready_candidate` | `reviewed_ready_candidate` |
| `603687.SH` | `reviewed_ready_candidate` | `reviewed_ready_candidate` |
| `000899.SZ` | `reviewed_ready_candidate` | `reviewed_ready_candidate` |

硬闸仍然关闭：

- `formal_front_filter_ready_count=0`
- `formal_data_write_allowed=false`
- `institution_rule_definition_allowed=false`
- `signal_generation_allowed=false`
- `backtest_execution_allowed=false`

当前下一步：

1. 若继续工程实现，应新增 `prepare_formal_front_filter_review_package(...)`。
2. 该入口只组装正式 front-filter review 输入包，不运行交易、不生成信号、不回测。
3. 只有 package 通过后，下一层才考虑正式 front-filter audit。

## 今日第九轮完成

- 新增 formal front-filter review package 准备入口：
  - `prepare_formal_front_filter_review_package(...)`
  - 输入为 `apply_malf_snapshot_manual_review_verdicts(...)` 输出。
  - 只收集 `manual_review_status=reviewed_ready_candidate` 且存在 `reviewed_snapshot_candidate` 的候选。
  - 生成 `front_filter_review_inputs`，但不执行 `tachibana_front_filter.run_front_filter(...)`。
- 输出边界：
  - `front_filter_execution_allowed=false`
  - `formal_front_filter_ready_count=0`
  - `next_action=action:run_formal_front_filter_audit_when_explicitly_requested`

真实数据 smoke 运行中，4 个 manual-approved 候选被组装为 review input：

```powershell
$env:PYTHONPATH='src'
@'
from data_sources.tdx_local import (
    apply_malf_snapshot_manual_review_verdicts,
    default_add_on_price_limit_shortlist_sample_entries,
    prepare_formal_front_filter_review_package,
    prepare_malf_snapshot_draft_review,
    rescreen_add_on_price_limit_post_industry_window,
    review_add_on_price_limit_post_label_daily_malf_structure,
    review_add_on_price_limit_post_label_intraday_reopen,
)
# rescreen -> intraday optional review -> daily MALF review -> draft prep -> manual verdicts -> package
'@ | python -
```

关键结果：

- `result=pass`
- `front_filter_review_input_count=4`
- `front_filter_review_hold_count=0`
- `front_filter_execution_allowed=false`
- `next_action=action:run_formal_front_filter_audit_when_explicitly_requested`

4 条 review input：

| ts_code | snapshot_quality_status | expected_front_filter_rule_id | front_filter_result |
| --- | --- | --- | --- |
| `603538.SH` | `reviewed_ready_candidate` | `Q-PRESSURE-ADJUST` | absent |
| `600310.SH` | `reviewed_ready_candidate` | `Q-PRESSURE-ADJUST` | absent |
| `603687.SH` | `reviewed_ready_candidate` | `Q-PRESSURE-ADJUST` | absent |
| `000899.SZ` | `reviewed_ready_candidate` | `Q-PRESSURE-ADJUST` | absent |

硬闸仍然关闭：

- `formal_front_filter_ready_count=0`
- `formal_data_write_allowed=false`
- `institution_rule_definition_allowed=false`
- `signal_generation_allowed=false`
- `backtest_execution_allowed=false`

当前下一步：

1. 若继续工程实现，应新增 formal front-filter audit 入口。
2. 该 audit 可以显式调用 front-filter 规则，但仍必须只产出 audit report，不产生交易、仓位或回测。
3. `reviewed_ready_candidate` 不能绕过 formal audit 直接变成 ready。

## 今日第十轮完成

- 新增 formal front-filter review audit 入口：
  - `audit_formal_front_filter_review_package(...)`
  - 输入为 `prepare_formal_front_filter_review_package(...)` 输出。
  - audit 会把 `reviewed_ready_candidate` 候选复制为临时 `snapshot_quality_status=ready` JSON，只在系统临时目录内调用 `tachibana_front_filter.run_front_filter(...)`。
  - 源 package 和候选本体不被提升为 ready，不写真实市场数据目录。
- 输出边界：
  - `front_filter_execution_allowed=false`
  - `formal_front_filter_ready_count=0`
  - `formal_data_write_allowed=false`
  - `institution_rule_definition_allowed=false`
  - `signal_generation_allowed=false`
  - `backtest_execution_allowed=false`
  - `next_action=action:prepare_qualification_record_draft_review_when_explicitly_requested`

新增测试覆盖：

- formal audit 可显式调用 front-filter 并只产出 audit report。
- review input 缺失时 blocked。
- 预期规则与实际 `qualification_rule_id` 不一致时 blocked。
- 输出不包含 `trade_accept / buy_signal / target_position` 等禁用字段。

验证结果：

```powershell
$env:PYTHONPATH='src'
python -m unittest tests.test_tdx_local_first_batch -v
# Ran 26 tests in 2.618s
# OK

python -m unittest discover -s tests
# Ran 215 tests in 14.187s
# OK
```

真实数据 smoke 使用 `Z:\new_tdx64` 与 `Z:\malf-data` 跑通：

- `rescreen pass 4`
- `daily pass 4 action:prepare_malf_snapshot_draft_review`
- `package pass 4 action:run_formal_front_filter_audit_when_explicitly_requested`
- `audit pass 4 0 action:prepare_qualification_record_draft_review_when_explicitly_requested`
- `rules ['Q-PRESSURE-ADJUST']`
- `forbidden False`

当前下一步：

1. 若继续工程实现，应新增 `prepare_qualification_record_draft_review(...)`。
2. 这一步仍只准备“资格记录草案复核包”，不写正式 qualification record，不给交易层开放读取。
3. formal audit 结果只能作为结构资格审计证据，仍不能生成交易信号、仓位或回测输入。

## 今日第十一轮完成

- 新增资格记录草案复核包入口：
  - `prepare_qualification_record_draft_review(...)`
  - 输入为 `audit_formal_front_filter_review_package(...)` 输出。
  - 仅接收 `formal_front_filter_audit_status=pass` 且 `front_filter_result=pass` 的 audit 结果。
  - 复用 `tachibana_front_filter.build_qualification_record_draft(...)` 生成草案材料，但本层会把所有可写入候选表/正式记录的动作关回 manual review。
- 输出边界：
  - `qualification_record_write_allowed=false`
  - `candidate_table_update_allowed=false`
  - `trading_layer_read_allowed=false`
  - `formal_front_filter_ready_count=0`
  - `formal_data_write_allowed=false`
  - `institution_rule_definition_allowed=false`
  - `signal_generation_allowed=false`
  - `backtest_execution_allowed=false`
  - `next_action=action:manual_review_qualification_record_drafts`

新增测试覆盖：

- formal front-filter audit pass 后可生成 `qualification_record_draft_review_inputs`。
- blocked audit result 会进入 hold，不生成草案复核输入。
- 输出不得包含 `action:fill_candidate_table`。
- 输出不得包含 `trade_accept / buy_signal / target_position` 等禁用字段。

验证结果：

```powershell
$env:PYTHONPATH='src'
python -m unittest tests.test_tdx_local_first_batch -v
# Ran 28 tests in 2.784s
# OK

python -m unittest discover -s tests
# Ran 217 tests in 16.885s
# OK
```

真实数据 smoke 使用 `Z:\new_tdx64` 与 `Z:\malf-data` 跑通：

- `rescreen pass 4`
- `audit pass 4 0`
- `qualification_review pass 4 0 action:manual_review_qualification_record_drafts`
- `rules ['Q-PRESSURE-ADJUST']`
- `statuses ['ready_for_manual_review']`
- `gates 0 False False False False False False False`
- `has_fill_candidate_table False`
- `forbidden False`

当前下一步：

1. 若继续工程实现，应新增 manual qualification record draft verdict 入口。
2. 该入口仍只记录人工复核结论，不写正式 qualification record，不更新 candidate table。
3. 只有后续明确的“正式记录写入审计/提交”层，才能讨论 qualification record 写入。

## 今日第十二轮完成

- 新增资格记录草案人工复核结论入口：
  - `apply_qualification_record_draft_manual_verdicts(...)`
  - 输入为 `prepare_qualification_record_draft_review(...)` 输出。
  - `approved_for_formal_record_write_audit` 只会生成 `approved_for_formal_record_write_audit_candidate`。
  - `rejected`、`needs_revision`、缺失 verdict 都保持 hold，不进入正式记录写入。
- 输出边界：
  - `qualification_record_write_allowed=false`
  - `candidate_table_update_allowed=false`
  - `trading_layer_read_allowed=false`
  - `formal_front_filter_ready_count=0`
  - `formal_data_write_allowed=false`
  - `institution_rule_definition_allowed=false`
  - `signal_generation_allowed=false`
  - `backtest_execution_allowed=false`
  - `next_action=action:prepare_formal_qualification_record_write_audit_when_explicitly_requested`

新增测试覆盖：

- approved manual verdict 只记录人工结论，不写正式 qualification record。
- rejected / needs_revision / missing verdict 保持 hold。
- 输出不得包含 `action:write_qualification_record` 或 `action:update_candidate_table`。
- 输出不得包含 `trade_accept / buy_signal / target_position` 等禁用字段。

验证结果：

```powershell
$env:PYTHONPATH='src'
python -m unittest tests.test_tdx_local_first_batch -v
# Ran 30 tests in 2.656s
# OK

python -m unittest discover -s tests
# Ran 219 tests in 14.838s
# OK
```

真实数据 smoke 使用 `Z:\new_tdx64` 与 `Z:\malf-data` 跑通：

- `rescreen pass 4`
- `qualification_review pass 4 0`
- `manual_qualification pass 4 0 action:prepare_formal_qualification_record_write_audit_when_explicitly_requested`
- `statuses ['approved_for_formal_record_write_audit_candidate']`
- `rules ['Q-PRESSURE-ADJUST']`
- `gates 0 False False False False False False False`
- `has_write_record False`
- `has_update_candidate False`
- `forbidden False`

当前下一步：

1. 若继续工程实现，应新增 formal qualification record write audit 入口。
2. 该入口仍应只审计“是否允许写正式 qualification record”，不立即写文件、不更新 candidate table。
3. 只有 audit 通过且后续明确提交层执行时，才进入真实记录写入。

## 今日第十三轮完成

- 新增正式资格记录写入审计入口：
  - `prepare_formal_qualification_record_write_audit(...)`
  - 输入为 `apply_qualification_record_draft_manual_verdicts(...)` 输出。
  - 仅接收 `qualification_record_manual_review_status=approved_for_formal_record_write_audit_candidate` 的人工复核结果。
  - 输出 `formal_record_write_audit_candidate`，但仍不写正式 qualification record。
- 输出边界：
  - `qualification_record_write_allowed=false`
  - `candidate_table_update_allowed=false`
  - `trading_layer_read_allowed=false`
  - `formal_front_filter_ready_count=0`
  - `formal_data_write_allowed=false`
  - `institution_rule_definition_allowed=false`
  - `signal_generation_allowed=false`
  - `backtest_execution_allowed=false`
  - `next_action=action:manual_commit_formal_qualification_records_when_explicitly_requested`

新增测试覆盖：

- approved manual verdict 可进入 formal write audit candidate。
- needs revision / missing approval 会进入 hold。
- 输出不得包含 `action:write_qualification_record` 或 `action:update_candidate_table`。
- 输出不得包含 `trade_accept / buy_signal / target_position` 等禁用字段。

验证结果：

```powershell
$env:PYTHONPATH='src'
python -m unittest tests.test_tdx_local_first_batch -v
# Ran 32 tests in 2.478s
# OK
```

当前下一步：

1. 若继续推进，应新增显式提交层，例如 `commit_formal_qualification_records_when_explicitly_requested(...)`。
2. 该提交层才允许讨论真实 qualification record 写入，但仍必须独立审计 candidate table 更新、交易层读取和制度/信号/回测硬闸。
3. 在提交层完成前，当前链路只证明“具备进入正式写入提交审核的资格”，不代表正式记录已经存在。
