# 2026-07-01 下一步工作计划

## 一、当前确认状态

截至 2026-07-01 当前检查，仓库最新主线已经推进到：

`candidate_table_trading_layer_readiness_audit_passed`

当前关键证据：

1. 最新提交为 `76ccd73 Add trading layer readiness audit`。
2. 当前分支 `Asteria-malf-pas/institution-facts-minimal-power` 已与远端 `origin` 对齐。
3. P5 已完成 spec、plan、实现、导出与测试：
   - `docs/superpowers/specs/2026-07-01-p5-trading-layer-readiness-audit-design.md`
   - `docs/superpowers/plans/2026-07-01-p5-trading-layer-readiness-audit.md`
   - `src/data_sources/tdx_local/first_batch.py`
   - `src/data_sources/tdx_local/__init__.py`
   - `tests/test_tdx_local_first_batch.py`
4. 当前测试口径为 `247 tests OK`。

P5 的真实含义是：

- formal candidate table 已通过 trading layer readiness audit；
- candidate table 已准备进入未来的 trading layer read gate review；
- 但 trading layer read 尚未开放；
- institution rule definition、signal、backtest 仍全部关闭。

## 二、当前仍关闭的硬闸

以下硬闸仍必须保持 `false`：

- `institution_rule_definition_allowed`
- `trading_layer_read_allowed`
- `signal_generation_allowed`
- `backtest_execution_allowed`
- `candidate_table_update_allowed`
- `qualification_record_write_allowed`
- `formal_data_write_allowed`

当前阶段不得把 P5 pass 理解为：

- 可以交易；
- 可以生成 signal；
- 可以生成仓位；
- 可以读取 trading layer；
- 可以定义正式制度规则；
- 可以运行 backtest。

P5 pass 只表示：

`ready_for_trading_layer_read_gate_review`

## 三、发现的文档口径漂移

子代理只读探索发现，代码和测试已经完成 P5，但部分进度文档仍停留在 P4/P5 待办口径。

需要后续收口的文档包括：

1. `docs/04_施工计划_当前进度版.md`
   - 当前仍以 `formal_candidate_table_update_performed` 为基线。
   - 尚未记录 P5 已实现并测试通过。

2. `docs/06_Roadmap_TodoList_后续路线图与待办.md`
   - 当前仍显示 P5 待办。
   - `P5：制度规则定义` 标题容易误导，因为刚完成的 P5 实际是 trading layer readiness audit。

3. `docs/daily-status/README.md`
   - 当前索引落后于实际 daily status 文件。

下一步优先级应先做文档状态收口，再进入新规格设计。

## 四、下一步主线判断

当前不建议直接进入 signal 或 backtest。

下一步最稳的主线是：

1. 先做 P5 状态收口与路线图更新。
2. 再写 P6 前置规格，而不是直接实现。
3. P6 前置规格应聚焦：
   - trading layer read gate / consumer contract；
   - execution constraint / execution feasibility 与 candidate table 的只读消费边界；
   - institution rule definition readiness 的阻断条件；
   - 不生成 signal、不生成仓位、不运行 backtest。

更具体地说，下一份规格建议命名为：

`docs/superpowers/specs/2026-07-01-p6-trading-layer-read-gate-contract-design.md`

如果决定先收口文档再开 P6 spec，则实施计划建议命名为：

`docs/superpowers/plans/2026-07-01-p5-status-reconciliation-and-roadmap-update.md`

## 五、下一步工作拆解

### Step 1：P5 状态收口

目标：

- 把主进度文档从 `formal_candidate_table_update_performed` 更新到 `candidate_table_trading_layer_readiness_audit_passed`。
- 明确 P5 已完成 spec、plan、实现、测试、推送。
- 明确 P5 未开放 trading layer。

建议触碰文件：

- `docs/04_施工计划_当前进度版.md`
- `docs/06_Roadmap_TodoList_后续路线图与待办.md`
- `docs/daily-status/README.md`

验证命令：

```powershell
git diff --check
```

### Step 2：P6 前置规格设计

目标：

- 定义 trading layer read gate / consumer contract。
- 明确它读取什么、审什么、阻断什么、输出什么。
- 明确 P6 仍不是 signal/backtest。
- 明确 Method/PM、Backtest Input、execution constraint/verdict 的关系。

建议新建：

`docs/superpowers/specs/2026-07-01-p6-trading-layer-read-gate-contract-design.md`

规格必须回答：

1. trading layer read gate 可以读取哪些 artifact？
2. 缺 Method/PM plan 时如何 blocked？
3. 缺 Backtest Input gate 时如何 blocked？
4. execution constraint snapshot 与 execution feasibility verdict 如何作为只读输入？
5. candidate table readiness audit 如何作为前置证明？
6. pass 是否仍保持 `trading_layer_read_allowed=false`，还是只推进到下一 review gate？

当前建议：P6 第一版仍保持 `trading_layer_read_allowed=false`，只设计 read gate contract，不真正打开读取。

### Step 3：P6 实施计划

只有在 P6 spec 评审通过后，才写实施计划。

建议新建：

`docs/superpowers/plans/2026-07-01-p6-trading-layer-read-gate-contract.md`

计划必须坚持 TDD：

- 先写 `pass` 测试；
- 再写 `blocked` 测试；
- 再写 forbidden field 测试；
- 再写 hard gate 保持关闭测试；
- 最后实现。

### Step 4：P6 实现候选位置

最可能触碰：

- `src/data_sources/tdx_local/first_batch.py`
- `src/data_sources/tdx_local/__init__.py`
- `tests/test_tdx_local_first_batch.py`

如果需要把 P5 与 A 股 execution policy research agenda 串成总状态视图，可能触碰：

- `src/ashare_intake_validator.py`
- `tests/test_ashare_intake_validator.py`

但第一版应尽量保持范围小，优先在 candidate table 后、trading layer 前保持只读 audit 边界。

## 六、下一阶段测试重点

新增审计阶段至少覆盖：

1. `pass`
2. `blocked`
3. 禁用字段校验
4. downstream hard gates 仍为 `false`

推荐 focused 命令：

```powershell
$env:PYTHONPATH='src'; $env:PYTHONDONTWRITEBYTECODE='1'; python -B -m unittest tests.test_tdx_local_first_batch -v
```

如果触碰 `ashare_intake_validator.py`，追加：

```powershell
$env:PYTHONPATH='src'; $env:PYTHONDONTWRITEBYTECODE='1'; python -B -m unittest tests.test_ashare_intake_validator -v
```

最终全量验证：

```powershell
$env:PYTHONPATH='src'; $env:PYTHONDONTWRITEBYTECODE='1'; python -B -m unittest discover -s tests -v
git diff --check
```

## 七、风险控制

下一阶段必须避免以下跑偏：

1. 把 P5 pass 解释成 trading layer 已开放。
2. 把 trading layer read gate 写成 signal generator。
3. 把 execution facts 写成 institution rules。
4. 把 T+1、涨跌停、停牌结论回流为：
   - `rhythm_meaning`
   - `tachibana_applicability`
   - `structure_suitable`
   - MALF 背景判断
5. 输出以下 forbidden fields：
   - `buy_signal`
   - `sell_signal`
   - `trade_accept`
   - `trade_reject`
   - `trade_defer`
   - `signal_decision`
   - `target_position`
   - `position_size`
   - `ashare_t1_action`
   - `limit_up_strategy`
   - `limit_down_strategy`
   - `industry_hot_score`
   - `liquidity_rank_as_applicability`
   - `rhythm_meaning_override`
   - `tachibana_applicability_override`

## 八、外部数据根约束

真实数据根仍在仓库外：

- `Z:\asteria-trading-labs-data`
- `Z:\malf-data`
- `Z:\new_tdx64`
- `Z:\tdx_offline_Data`

默认约束：

- 不复制 DuckDB 入库；
- 不复制通达信原始二进制入库；
- 不提交真实市场导出数据；
- 不写 `Z:\asteria-trading-labs-data`，除非命令明确是受控导出/写入；
- 所有测试继续使用临时目录和 fixtures。

## 九、建议今日下一动作

建议按以下顺序继续：

1. 提交本文件作为 2026-07-01 下一步工作计划。
2. 更新 `docs/04_施工计划_当前进度版.md`，记录 P5 已完成。
3. 更新 `docs/06_Roadmap_TodoList_后续路线图与待办.md`，把 P5 从待办改为已完成/已收口，并把下一阶段改为 P6 read gate contract。
4. 更新 `docs/daily-status/README.md`，补上 2026-06-30 与 2026-07-01 条目。
5. 开始写 P6 spec，而不是直接实现。

一句话收口：

> 当前系统已完成 P5 trading layer readiness audit，但 trading layer 仍未开放；下一步应先做 P5 文档状态收口，再设计 P6 trading layer read gate / consumer contract，继续守住 institution rule、signal、backtest 三道硬闸。

## 十、执行进展

截至本轮继续施工，已按本计划完成：

1. 更新 `docs/04_施工计划_当前进度版.md`，将当前系统位置收口为 `candidate_table_trading_layer_readiness_audit_passed`。
2. 更新 `docs/06_Roadmap_TodoList_后续路线图与待办.md`，将 P5 标记为已完成的 trading layer readiness audit，并将 P6 调整为 trading layer read gate / consumer contract。
3. 更新 `docs/daily-status/README.md`，补齐 2026-06-30 与 2026-07-01 的 daily status 索引。
4. 新增 `docs/superpowers/specs/2026-07-01-p6-trading-layer-read-gate-contract-design.md`，完成 P6 前置规格草案。

当前仍未进入实现阶段；P6 仍停留在规格设计层，不开放真实 `trading_layer_read_allowed`。
