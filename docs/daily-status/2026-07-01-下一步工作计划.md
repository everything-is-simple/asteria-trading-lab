# 2026-07-01 下一步工作计划

## 一、当前确认状态

截至 2026-07-01 当前检查，仓库最新主线已经推进到：

`institution_rule_definition_opened_for_rule_definition_only`

当前关键证据：

1. 当前分支为 `Asteria-malf-pas/institution-facts-minimal-power`。
2. 当前分支 `Asteria-malf-pas/institution-facts-minimal-power` 已与远端 `origin` 对齐。
3. P5/P6/P7a/P7b/P7c/P7d 已完成 spec、plan、实现、导出与 focused 测试：
   - `docs/superpowers/specs/2026-07-01-p7-institution-rule-definition-readiness-design.md`
   - `docs/superpowers/plans/2026-07-01-p7-institution-rule-definition-readiness.md`
   - `docs/superpowers/specs/2026-07-01-p7b-institution-rule-draft-review-gate-design.md`
   - `docs/superpowers/plans/2026-07-01-p7b-institution-rule-draft-review-gate.md`
   - `docs/superpowers/specs/2026-07-01-p7c-institution-rule-definition-contract-review-design.md`
   - `docs/superpowers/plans/2026-07-01-p7c-institution-rule-definition-contract-review.md`
   - `docs/superpowers/specs/2026-07-01-p7d-explicit-institution-rule-definition-open-gate-design.md`
   - `docs/superpowers/plans/2026-07-01-p7d-explicit-institution-rule-definition-open-gate.md`
   - `src/data_sources/tdx_local/first_batch.py`
   - `src/data_sources/tdx_local/__init__.py`
   - `tests/test_tdx_local_first_batch.py`
4. P7a/P7b/P7c/P7d focused 测试口径为 `tests.test_tdx_local_first_batch` 通过。
5. 当前全量测试口径为 `271 tests OK`。

当前真实含义是：

- formal candidate table 已通过 trading layer readiness audit；
- P6 read gate contract audit 已通过全量测试；
- P7a 制度规则定义准备审计已通过 focused 测试；
- P7b 制度规则草案复核闸已通过 focused 测试；
- P7c 制度规则定义 contract review 已通过 focused 测试；
- P7d 显式制度规则定义开放 gate 已通过 focused 测试；
- T+1、涨跌停、停复牌只作为 `draft_input_only=True` 的规则草案输入；
- 制度规则定义入口已开放为 rule-definition-only；
- `institution_rule_definition_allowed=True` 只允许 rule-definition-only；
- 真实 trading layer read、正式规则文件生成、signal、backtest 仍全部关闭。

## 二、当前仍关闭的硬闸

P7d 后 `institution_rule_definition_allowed=True` 只表示 rule-definition-only，不等于 trading layer、signal 或 backtest 放行。

以下硬闸仍必须保持 `false`：

- `trading_layer_read_allowed`
- `signal_generation_allowed`
- `backtest_execution_allowed`
- `candidate_table_update_allowed`
- `qualification_record_write_allowed`
- `formal_data_write_allowed`

当前阶段不得把 P5 pass 理解为：

- 可以交易；
- 可以生成 signal；
- 可以生成仓位；
- 可以读取 trading layer；
- 可以定义正式制度规则；
- 可以运行 backtest。

P5 pass 只表示：

`ready_for_trading_layer_read_gate_review`

P6 pass 只表示：

`ready_for_trading_layer_read_contract_review`

P7a pass 只表示：

`ready_for_institution_rule_definition_draft_review`

P7b pass 只表示：

`ready_for_institution_rule_definition_contract_review`

P7c pass 只表示：

`ready_for_explicit_institution_rule_definition_open_gate_review`

P7d pass 只表示：

`institution_rule_definition_opened_for_rule_definition_only`

## 三、当前口径修正

本轮已确认：此前文档中“P7 已完成”的说法过满。

准确口径是：

- P7a 制度规则定义准备审计已完成。
- P7b 制度规则草案复核闸已完成 focused 验证。
- P7c 制度规则定义 contract review 已完成 focused 与全量验证。
- P7d 显式制度规则定义开放 gate 已完成 focused 与全量验证。
- 当前系统位置是 `institution_rule_definition_opened_for_rule_definition_only`。
- 下一步不是 P8 signal/backtest，而是正式制度规则定义文件/契约本身。

## 四、下一步主线判断

当前不建议直接进入 signal 或 backtest。

下一步最稳的主线是：

1. 起草正式制度规则定义 spec。
2. 明确规则定义只消费 P7d rule-definition-only open gate report 与三类 contract-ready reviewed draft。
3. 明确正式规则定义仍不得直接生成 signal/backtest。
4. 明确 trading layer read 仍需后续独立 gate。

## 五、下一步工作拆解

### Step 1：正式制度规则定义 spec

目标：

- 明确正式规则定义读取什么。
- 明确正式规则定义审什么。
- 明确正式规则定义阻断什么。
- 明确正式规则定义输出什么。
- 明确正式规则定义仍不得顺带打开 trading layer、signal、backtest。

### Step 2：formal rule definition implementation plan

目标：

- 继续沿用 `first_batch.py` 附近的只读 audit 位置。
- 继续使用 `tests/test_tdx_local_first_batch.py`。
- 先写 RED 测试，再实现。

### Step 3：formal rule definition TDD 实现候选位置

最可能触碰：

- `src/data_sources/tdx_local/first_batch.py`
- `src/data_sources/tdx_local/__init__.py`
- `tests/test_tdx_local_first_batch.py`

第一版仍应保持范围小，不进入 `ashare_intake_validator.py` CLI 管线。

## 六、下一阶段测试重点

新增审计阶段至少覆盖：

1. `pass`
2. `blocked`
3. 禁用字段校验
4. downstream hard gates 仍为 `false`

推荐 focused 命令：

```powershell
$env:PYTHONPATH='src'; $env:PYTHONDONTWRITEBYTECODE='1'; python -B -m unittest tests.test_tdx_local_first_batch -v
```

如果触碰 `ashare_intake_validator.py`，追加：

```powershell
$env:PYTHONPATH='src'; $env:PYTHONDONTWRITEBYTECODE='1'; python -B -m unittest tests.test_ashare_intake_validator -v
```

最终全量验证：

```powershell
$env:PYTHONPATH='src'; $env:PYTHONDONTWRITEBYTECODE='1'; python -B -m unittest discover -s tests -v
git diff --check
```

## 七、风险控制

下一阶段必须避免以下跑偏：

1. 把 P5 pass 解释成 trading layer 已开放。
2. 把 trading layer read gate 写成 signal generator。
3. 把 execution facts 写成 institution rules。
4. 把 T+1、涨跌停、停牌结论回流为：
   - `rhythm_meaning`
   - `tachibana_applicability`
   - `structure_suitable`
   - MALF 背景判断
5. 输出以下 forbidden fields：
   - `buy_signal`
   - `sell_signal`
   - `trade_accept`
   - `trade_reject`
   - `trade_defer`
   - `signal_decision`
   - `target_position`
   - `position_size`
   - `ashare_t1_action`
   - `limit_up_strategy`
   - `limit_down_strategy`
   - `industry_hot_score`
   - `liquidity_rank_as_applicability`
   - `rhythm_meaning_override`
   - `tachibana_applicability_override`

## 八、外部数据根约束

真实数据根仍在仓库外：

- `Z:\asteria-trading-labs-data`
- `Z:\malf-data`
- `Z:\new_tdx64`
- `Z:\tdx_offline_Data`

默认约束：

- 不复制 DuckDB 入库；
- 不复制通达信原始二进制入库；
- 不提交真实市场导出数据；
- 不写 `Z:\asteria-trading-labs-data`，除非命令明确是受控导出/写入；
- 所有测试继续使用临时目录和 fixtures。

## 九、建议今日下一动作

P7d 主线已经按 TDD 完成实现与全量验证。下一动作是进入正式制度规则定义：

1. 起草 formal rule definition spec，明确读取 P7d report 与 contract-ready reviewed draft input。
2. 明确 formal rule definition 仍不定义交易信号、不运行 backtest。
3. 编写 formal rule definition implementation plan。
4. 按 TDD 覆盖 pass / blocked / forbidden field / hard gates false。

一句话收口：

> 当前系统已完成 P7d 显式制度规则定义开放 gate 的全量验证；下一步进入 formal rule definition，仍继续守住 trading layer read、signal、backtest 硬闸。

## 十、执行进展

截至本轮继续施工，已按本计划完成：

1. 当时更新 `docs/04_施工计划_当前进度版.md`，将 P5 阶段位置收口为 `candidate_table_trading_layer_readiness_audit_passed`；后续已继续推进到 P7d 的 `institution_rule_definition_opened_for_rule_definition_only`。
2. 更新 `docs/06_Roadmap_TodoList_后续路线图与待办.md`，将 P5 标记为已完成的 trading layer readiness audit，并将 P6 调整为 trading layer read gate / consumer contract。
3. 更新 `docs/daily-status/README.md`，补齐 2026-06-30 与 2026-07-01 的 daily status 索引。
4. 新增 `docs/superpowers/specs/2026-07-01-p6-trading-layer-read-gate-contract-design.md`，完成 P6 前置规格草案。
5. 完成 P6 spec review，将 P6 spec 状态定为“已评审定版，可写 implementation plan；尚未实现”。
6. 精修 P5/P6 文档状态，明确 P5 spec 已实现归档，P6 roadmap 只完成规格设计，尚未实现。
7. 新建 `docs/superpowers/plans/2026-07-01-p6-trading-layer-read-gate-contract.md`，要求先写 pass / blocked / forbidden field / hard gate / no trading layer read opened 测试。
8. 按 P6 implementation plan 增加 RED 测试，确认新入口未导出时测试失败。
9. 实现 `audit_trading_layer_read_gate_contract_when_explicitly_requested` 并从 `src/data_sources/tdx_local/__init__.py` 导出。
10. focused 验证通过：
    `$env:PYTHONPATH='src'; $env:PYTHONDONTWRITEBYTECODE='1'; python -B -m unittest tests.test_tdx_local_first_batch -v`
11. 全量验证通过：
    `$env:PYTHONPATH='src'; $env:PYTHONDONTWRITEBYTECODE='1'; python -B -m unittest discover -s tests -v`
12. 新增 P7a spec：
    `docs/superpowers/specs/2026-07-01-p7-institution-rule-definition-readiness-design.md`
13. 新增 P7a implementation plan：
    `docs/superpowers/plans/2026-07-01-p7-institution-rule-definition-readiness.md`
14. 按 P7a implementation plan 增加 RED 测试，确认新入口未导出时测试失败。
15. 实现 `audit_institution_rule_definition_readiness_when_explicitly_requested` 并从 `src/data_sources/tdx_local/__init__.py` 导出。
16. focused 验证通过：
    `$env:PYTHONPATH='src'; $env:PYTHONDONTWRITEBYTECODE='1'; python -B -m unittest tests.test_tdx_local_first_batch -v`
17. 全量验证通过：
    `$env:PYTHONPATH='src'; $env:PYTHONDONTWRITEBYTECODE='1'; python -B -m unittest discover -s tests -v`
18. 新增 P7b spec：
    `docs/superpowers/specs/2026-07-01-p7b-institution-rule-draft-review-gate-design.md`
19. 新增 P7b implementation plan：
    `docs/superpowers/plans/2026-07-01-p7b-institution-rule-draft-review-gate.md`
20. 按 P7b implementation plan 增加 RED 测试，确认新入口未导出时测试失败。
21. 实现 `audit_institution_rule_definition_draft_review_gate_when_explicitly_requested` 并从 `src/data_sources/tdx_local/__init__.py` 导出。
22. focused 验证通过：
    `$env:PYTHONPATH='src'; $env:PYTHONDONTWRITEBYTECODE='1'; python -B -m unittest tests.test_tdx_local_first_batch -v`
23. 全量验证通过：
    `$env:PYTHONPATH='src'; $env:PYTHONDONTWRITEBYTECODE='1'; python -B -m unittest discover -s tests -v`
24. 新增 P7c spec：
    `docs/superpowers/specs/2026-07-01-p7c-institution-rule-definition-contract-review-design.md`
25. 新增 P7c implementation plan：
    `docs/superpowers/plans/2026-07-01-p7c-institution-rule-definition-contract-review.md`
26. 按 P7c implementation plan 增加 RED 测试，确认新入口未导出时测试失败。
27. 实现 `audit_institution_rule_definition_contract_review_when_explicitly_requested` 并从 `src/data_sources/tdx_local/__init__.py` 导出。
28. focused 验证通过：
    `$env:PYTHONPATH='src'; $env:PYTHONDONTWRITEBYTECODE='1'; python -B -m unittest tests.test_tdx_local_first_batch -v`
29. 全量验证通过：
    `$env:PYTHONPATH='src'; $env:PYTHONDONTWRITEBYTECODE='1'; python -B -m unittest discover -s tests -v`
30. 新增 P7d spec：
    `docs/superpowers/specs/2026-07-01-p7d-explicit-institution-rule-definition-open-gate-design.md`
31. 新增 P7d implementation plan：
    `docs/superpowers/plans/2026-07-01-p7d-explicit-institution-rule-definition-open-gate.md`
32. 按 P7d implementation plan 增加 RED 测试，确认新入口未导出时测试失败。
33. 实现 `audit_explicit_institution_rule_definition_open_gate_when_explicitly_requested` 并从 `src/data_sources/tdx_local/__init__.py` 导出。
34. focused 验证通过：
    `$env:PYTHONPATH='src'; $env:PYTHONDONTWRITEBYTECODE='1'; python -B -m unittest tests.test_tdx_local_first_batch -v`
35. 全量验证通过：
    `$env:PYTHONPATH='src'; $env:PYTHONDONTWRITEBYTECODE='1'; python -B -m unittest discover -s tests -v`

当前已完成 P7d 实现与全量验证；P7d pass 只表示 `institution_rule_definition_opened_for_rule_definition_only`，不开放真实 `trading_layer_read_allowed`，也不开放 signal 或 backtest。下一步是正式制度规则定义。

## 十一、今日完成定义

今天优先交付的不是 trading signal，也不是 backtest，而是清楚可执行的 P7d 显式制度规则定义开放 gate：

1. P7a spec 已明确 T+1、涨跌停、停复牌只作为规则草案输入。
2. P7a implementation plan 已完成。
3. P7a 已按 TDD 覆盖 pass / blocked / forbidden field / hard gates false。
4. P7a focused 与全量验证已通过。
5. P7b spec 已明确草案质量、字段契约、证据引用与边界复核。
6. P7b implementation plan 已完成。
7. P7b 已按 TDD 覆盖 pass / blocked / draft quality / forbidden field / hard gates false。
8. P7b focused 与全量验证已通过。
9. P7c spec 已明确 contract review 读取、审计、阻断与输出。
10. P7c implementation plan 已完成。
11. P7c 已按 TDD 覆盖 pass / blocked / contract quality / forbidden field / hard gates false。
12. P7c focused 与全量验证已通过。
13. P7d spec 已明确显式 open-gate 读取、审计、阻断与输出。
14. P7d implementation plan 已完成。
15. P7d 已按 TDD 覆盖 pass / blocked / bad decision / forbidden field / opens only rule definition gate。
16. P7d focused 与全量验证已通过。
17. 硬闸状态：
   - `institution_rule_definition_allowed=True`
   - `trading_layer_read_allowed=False`
   - `signal_generation_allowed=False`
   - `backtest_execution_allowed=False`
