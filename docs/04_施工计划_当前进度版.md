# 04_施工计划 — 当前进度版

**版本**: v0.4
**日期**: 2026-06-30
**当前分支**: `Asteria-malf-pas/institution-facts-minimal-power`

## 1. 当前系统位置

当前系统已经从最初的 `execution_policy_research_agenda` 推进到资格记录审计链路后段。

截至 2026-06-30，系统的准确位置是：

`formal_record_ready_for_persistence`

这表示：

- 正式资格记录已经完成审计准备、人工裁决、写入前审计与持久化包准备。
- 当前只是准备好 `qualification_record_persistence_package`。
- 尚未执行真实持久化写入。
- 尚未更新 candidate table。
- 尚未开放 trading layer。
- 尚未开放 signal generation 或 backtest execution。

最新验证状态：

- 全量测试：`223 tests OK`
- 关键语义：`qualification_record_persistence_package_prepared=True`
- 关键语义：`qualification_record_persistence_performed=False`
- 关键闸门：`qualification_record_write_allowed=False`
- 关键闸门：`candidate_table_update_allowed=False`
- 关键闸门：`trading_layer_read_allowed=False`
- 关键闸门：`signal_generation_allowed=False`
- 关键闸门：`backtest_execution_allowed=False`

## 2. 已完成阶段

### Phase A：立花原始研究

- 24 个月资料数字化
- 月报、章节精读、术语表、映射表
- Pioneer v0.1 回测与 15 笔交易段复盘

### Phase B：MALF 前置过滤器

- 结构资格分层
- Method / PM 边界
- 样本表试填链路
- 真实样本前置过滤器跑通

### Phase C：本地数据主账本

- TDX `.day` / `.txt` 读取
- DuckDB 只读接入
- 首批真实样本接入包
- 最小制度事实包

### Phase D：首批制度审计链路

- execution feasibility verdicts
- verdict merge
- outcomes
- execution policy candidates
- review merge
- archive
- research prep
- research agenda

### Phase E：第十四轮审计链路扩展

- add-on / price-limit post-label 重筛
- intraday reopen review
- daily MALF structure review
- MALF snapshot draft review
- manual MALF verdict
- formal front-filter review package
- formal front-filter audit
- qualification record draft review
- qualification record draft manual verdict
- formal qualification record write audit

### Phase F：第十五轮语义校准

- 将误导性的 commit 语义修正为 persistence package 语义。
- 明确当前不是正式落库完成。
- 明确当前只是资格记录持久化包已准备。
- 保持 candidate table、trading layer、signal、backtest 全部关闭。

## 3. 当前正在完成的任务

当前任务不是规则定义，也不是交易层接入。

当前任务是：

> 将正式资格记录持久化包，推进到 candidate table update audit 的前置审计层。

这一步的正确边界是：

- 可以检查 persistence package 是否具备映射到 candidate table 的资格。
- 可以生成 candidate table update audit package。
- 不直接写 candidate table。
- 不打开 trading layer。
- 不产生买卖信号。
- 不执行回测。

## 4. 下一步急需完成

### P0：施工计划文档同步

本文件作为施工计划正本，原地升级到 v0.4。

不再新建并列的 `04_施工计划.md`，避免出现两个施工计划来源。

### P1：candidate table update audit

新增显式触发入口：

`prepare_candidate_table_update_audit_when_explicitly_requested`

该入口只做审计准备，不做真实写入。

测试最低要求：

- pass case
- blocked case
- 禁用字段校验
- 三道硬闸继续为 false
- 不允许混入交易信号、仓位、回测字段

验证命令：

```powershell
$env:PYTHONPATH='src'
python -m unittest tests.test_tdx_local_first_batch -v
python -m unittest discover -s tests
```

### P2：样本扩充

继续执行 post-label 窗口重筛，补齐四类样本：

- `meaningful`
- `limited`
- `unknown`
- `not_meaningful`

样本扩充仍属于研究审计，不属于交易许可。

### P3：制度研究准备层收口

继续整理 `execution_policy_research_agenda` 的三类议题：

- `t1`
- `price_limit`
- `suspension_resume`

该阶段完成后，才允许讨论制度规则草案。

## 5. 暂不启动的部分

以下部分现在不启动：

- candidate table 真实写入
- trading layer read
- Signal 实现
- A 股完整规则定义
- A 股完整回测执行层
- Pioneer v0.2 回测重跑

另有一条口径继续固定：

- `PAS` 在本项目中已明确搁置，当前不纳入施工排期，也不纳入进度估算。

## 6. 对 Mistral 进度估算的校准

Mistral Large 的工程节奏判断可作为参考，但需要校准：

- “90% 完成”只适用于当前审计链路骨架，不适用于完整可回测系统。
- 若以完整可回测系统计算，当前更接近 40% 到 50%。
- 若以资格记录进入候选表之前的主线计算，当前更接近 60% 到 70%。
- 第十五轮修正后，不能再把 persistence package 称为 commit 或落库完成。

因此，下一步不应直接做真实写入，而应先补 candidate table update audit。

## 7. 当前风险

- 文档状态容易落后于代码推进。
- persistence package 与真实持久化写入容易被混称。
- candidate table update audit 容易被误解为真实更新。
- 制度事实包仍是最小通电，不足以直接写涨跌停规则。
- 真实样本数量仍偏少。

## 8. 今日可交付判断

到 2026-06-30 为止，项目可以明确说：

- 结构资格主线已经立住。
- 首批真实 A 股样本已经通到制度审计与资格记录准备层。
- 第十四轮审计链路已完成。
- 第十五轮语义校准已完成。
- 系统仍严格停在审计边界内。
- 今天最后一个文档任务是把施工计划正本同步到 v0.4。
