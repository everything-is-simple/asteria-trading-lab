# 04_施工计划 — 当前进度版

**版本**: v1.2
**日期**: 2026-07-01
**当前分支**: `Asteria-malf-pas/institution-facts-minimal-power`
**文档性质**: 已发生进度与当前系统位置记录，不承接未来待办

## 1. 当前系统位置

当前系统已经从最初的 `execution_policy_research_agenda` 推进到资格记录审计链路后段。

截至 2026-07-01，系统的准确位置是：

`candidate_table_trading_layer_readiness_audit_passed`

这表示：

- 正式资格记录已经完成审计准备、人工裁决、写入前审计与持久化包准备。
- 当前只是准备好 `qualification_record_persistence_package`。
- 已完成 M1：从 `qualification_record_persistence_package` 生成 candidate table update audit package。
- 已完成 M2：前置过滤器样本目录补齐四类 `rhythm_meaning` 覆盖，并新增四类最低样本数审计。
- 已完成 P4a：在调用方提供的临时/staging 目录中验证资格记录真实文件 IO。
- 已完成 P4b：在调用方提供的 staging 目录中生成 candidate table draft JSONL 与 manifest。
- 已完成 P4c：实现显式确认后的 formal data root candidate table JSONL 写入路径，并在临时 formal data root 中验证 backup 与 rollback。
- 已完成 P5：实现 formal candidate table 到 trading layer read gate review 之前的只读 readiness audit。
- 尚未对真实生产路径 `Z:\asteria-trading-labs-data` 执行人工确认写入。
- 尚未开放真实 trading layer read。
- 尚未开放 signal generation 或 backtest execution。

最新验证状态：

- 全量测试：`247 tests OK`
- 关键语义：`qualification_record_persistence_package_prepared=True`
- 关键语义：`qualification_record_persistence_performed=False`
- 关键语义：`candidate_table_update_audit_result=pass`
- 关键语义：`candidate_table_update_package_prepared=True`
- 关键语义：`candidate_table_update_performed=False`
- 关键语义：`qualification_record_persistence_performed=True`
- 关键语义：`qualification_record_persistence_target=staging`
- 关键语义：`candidate_table_update_performed=False`
- 关键语义：`candidate_table_update_performed=True`
- 关键语义：`candidate_table_update_target=staging`
- 关键语义：`candidate_table_update_performed=True`
- 关键语义：`candidate_table_update_target=formal_data_root`
- 关键语义：`confirm_formal_write=True` 才允许 P4c 写入
- 关键语义：`candidate_table_trading_layer_readiness_audit_result=pass`
- 关键语义：`candidate_table_trading_layer_readiness_checked=True`
- 关键语义：`candidate_table_trading_layer_readiness_status=ready_for_trading_layer_read_gate_review`
- 关键语义：`candidate_table_update_allowed=False`
- 关键语义：`rhythm_sample_catalog_sample_count=18`
- 关键语义：`rhythm_sample_catalog_missing_rhythm_meanings=[]`
- 关键语义：`rhythm_sample_catalog_undercovered_rhythm_meanings={}`
- 关键语义：`industry_time_alignment_result=pass`
- 关键语义：`industry_time_alignment_blocked_count=0`
- 关键语义：`execution_policy_research_closure_status=research_materials_closed_for_p3`
- 关键语义：`t1_research_status=prepared_for_rule_definition_later`
- 关键语义：`price_limit_research_status=prepared_with_sample_gap`
- 关键语义：`suspension_resume_research_status=prepared_as_carry_forward_constraint`
- 关键闸门：`qualification_record_write_allowed=False`
- 关键闸门：`candidate_table_update_allowed=False`
- 关键闸门：`institution_rule_definition_allowed=False`
- 关键闸门：`trading_layer_read_allowed=False`
- 关键闸门：`signal_generation_allowed=False`
- 关键闸门：`backtest_execution_allowed=False`

## 2. 已完成阶段

### Phase A：立花原始研究

- 24 个月资料数字化
- 月报、章节精读、术语表、映射表
- Pioneer v0.1 回测与 15 笔交易段复盘

### Phase B：MALF 前置过滤器

- 结构资格分层
- Method / PM 边界
- 样本表试填链路
- 真实样本前置过滤器跑通

### Phase C：本地数据主账本

- TDX `.day` / `.txt` 读取
- DuckDB 只读接入
- 首批真实样本接入包
- 最小制度事实包

### Phase D：首批制度审计链路

- execution feasibility verdicts
- verdict merge
- outcomes
- execution policy candidates
- review merge
- archive
- research prep
- research agenda

### Phase E：第十四轮审计链路扩展

- add-on / price-limit post-label 重筛
- intraday reopen review
- daily MALF structure review
- MALF snapshot draft review
- manual MALF verdict
- formal front-filter review package
- formal front-filter audit
- qualification record draft review
- qualification record draft manual verdict
- formal qualification record write audit

### Phase F：第十五轮语义校准

- 将误导性的 commit 语义修正为 persistence package 语义。
- 明确当前不是正式落库完成。
- 明确当前只是资格记录持久化包已准备。
- 保持 candidate table、trading layer、signal、backtest 全部关闭。

### Phase G：M1 candidate table update audit

- 新增 `prepare_candidate_table_update_audit_when_explicitly_requested`。
- 输入来自 `qualification_record_persistence_package`。
- 输出 candidate table update audit package。
- 明确 `candidate_table_update_performed=False`。
- 明确 `candidate_table_update_allowed=False`。
- 明确 trading layer、signal、backtest 全部关闭。

### Phase H：M2 样本扩充与四类覆盖

- 将机器样本目录扩充到 18 条。
- 四类 `rhythm_meaning` 覆盖为：`meaningful=2`、`limited=10`、`unknown=2`、`not_meaningful=4`。
- `audit_rhythm_sample_catalog()` 输出四类覆盖计数、缺失类与低于最低样本数的类。
- 四类最低样本数默认要求各不少于 2 条。
- 对带有行业标签时间字段的样本行，阻断 `current_industry_valid_from > sample_window_end` 的未来标签污染。
- 该阶段仍属于前置过滤器样本研究审计，不写 candidate table，不开放 trading layer、signal 或 backtest。

### Phase I：P3 制度研究准备层收口

- 新增 [Tachibana-A股制度研究准备层收口报告-v0.1.md](./tachibana/index/Tachibana-A股制度研究准备层收口报告-v0.1.md)。
- 收口 `t1 / price_limit / suspension_resume` 三类研究材料。
- 明确 `execution_policy_research_closure_status=research_materials_closed_for_p3`。
- 明确 P3 仍不定义制度规则，不生成 signal，不开放 backtest。
- 明确制度规则定义已后移到 P7，仍需等待 P6 read gate contract 与更多样本证据。

### Phase J：P4a staging 资格记录持久化验证

- 新增 `write_qualification_records_to_staging_when_explicitly_requested`。
- 输入来自已通过的 candidate table update audit package。
- 输出 `qualification-records-v0.1/records/<qualification_record_id>.json` 与 `qualification-records-v0.1/manifest.json`。
- 写入顺序为先写 record 临时文件并原子替换，再最后写 manifest，避免 manifest 先存在但 records 不完整。
- 明确 `qualification_record_persistence_performed=True` 只表示 staging 持久化已执行。
- 明确 `qualification_record_persistence_target=staging`。
- 明确 `candidate_table_update_performed=False`。
- 明确 `candidate_table_update_allowed=False`。
- 明确 trading layer、signal、backtest 全部关闭。

### Phase K：P4b staging candidate table 更新验证

- 新增 `update_candidate_table_from_staged_qualification_records_when_explicitly_requested`。
- 输入来自 P4a 生成的 `qualification-records-v0.1/manifest.json`。
- 输出 `candidate-table-v0.1/candidate-table-draft.jsonl` 与 `candidate-table-v0.1/manifest.json`。
- 写入顺序为先写 `candidate-table-v0.1.__tmp__` 临时目录，再替换为 `candidate-table-v0.1`。
- 在 Windows 上避免直接 replace 非空目录；先完成临时目录，再移除旧目录并 rename。
- 明确 `candidate_table_update_performed=True` 只表示 staging candidate table draft 已生成。
- 明确 `candidate_table_update_target=staging`。
- 明确 `candidate_table_update_allowed=False`。
- 明确正式 `data_root` candidate table 未更新。
- 明确 trading layer、signal、backtest 全部关闭。

### Phase L：P4c formal data root candidate table 写入验证

- 新增 `write_candidate_table_to_formal_data_root_when_explicitly_confirmed`。
- 输入来自 P4b 生成的 `candidate-table-v0.1/manifest.json`。
- 仅在 `confirm_formal_write=True` 时执行；未显式确认时立即 block，且不读取 staging 文件、不写入 formal data root。
- 输出 `<formal_data_root>/ashare/candidate-table-v0.1/candidate-table.jsonl` 与 `<formal_data_root>/ashare/candidate-table-v0.1/manifest.json`。
- 若 formal candidate table 已存在，先备份到 `<formal_data_root>/ashare/candidate-table-v0.1.backup.<timestamp>/`。
- 写入顺序为先写 `candidate-table-v0.1.__tmp__` 临时目录，再替换为 `candidate-table-v0.1`；manifest 在临时目录内最后写入。
- 失败时清理临时目录，保留旧 live 目录，并保留可读 backup。
- 明确 `candidate_table_update_performed=True` 只表示 formal data root candidate table 写入入口已执行。
- 明确 `candidate_table_update_target=formal_data_root`。
- 明确 `candidate_table_update_allowed=False`。
- 明确 trading layer、signal、backtest 全部关闭。
- 本轮测试仅使用临时 formal data root；未对真实生产路径 `Z:\asteria-trading-labs-data` 执行写入。

### Phase M：P5 trading layer readiness audit

- 新增 `audit_trading_layer_readiness_for_candidate_table_when_explicitly_requested`。
- 输入来自 P4c formal candidate table 的 `manifest.json`。
- 只读取 `<formal_data_root>/ashare/candidate-table-v0.1/manifest.json` 与 `candidate-table.jsonl`。
- 审计 manifest identity、row count、JSONL 格式、row id 唯一性、formal target、forbidden fields 与 hard gates。
- pass 结果只表示 `ready_for_trading_layer_read_gate_review`。
- blocked 结果覆盖 missing manifest、missing JSONL、malformed JSONL、forbidden field 等情况。
- 明确 `institution_rule_definition_allowed=False`。
- 明确 `trading_layer_read_allowed=False`。
- 明确 `signal_generation_allowed=False`。
- 明确 `backtest_execution_allowed=False`。
- 明确 P5 不生成 signal、不生成仓位、不运行 backtest、不定义正式制度规则。
- 验证：`tests.test_tdx_local_first_batch` 新增 P5 pass / blocked / forbidden field / missing JSONL / malformed JSONL / downstream gates 测试。
- 全量验证：`247 tests OK`。

## 3. 已确认未发生事项

以下事项截至 2026-07-01 尚未发生：

- 对真实生产路径 `Z:\asteria-trading-labs-data` 执行人工确认写入
- 真实 trading layer read
- Signal 实现
- A 股完整规则定义
- A 股完整回测执行层
- Pioneer v0.2 回测重跑

另有一条口径继续固定：

- `PAS` 在本项目中已明确搁置，当前不纳入施工排期，也不纳入进度估算。

## 4. 对 Mistral 进度估算的校准

Mistral Large 的工程节奏判断可作为参考，但需要校准：

- “90% 完成”只适用于当前审计链路骨架，不适用于完整可回测系统。
- 若以完整可回测系统计算，当前更接近 40% 到 50%。
- 若以资格记录进入候选表之前的主线计算，当前更接近 60% 到 70%。
- 第十五轮修正后，不能再把 persistence package 称为 commit 或落库完成。

因此，2026-07-01 的口径校准结论是：candidate table update audit 已完成，P4a staging 资格记录持久化已验证，P4b staging candidate table draft 已生成，P4c formal data root candidate table 写入入口已实现并在临时 formal data root 验证 backup/rollback；P5 formal candidate table trading layer readiness audit 已实现并通过测试；但真实生产路径 `Z:\asteria-trading-labs-data` 尚未执行人工确认写入，真实 trading layer read 仍未发生。

未来路线图与待办已移至：

[06_Roadmap_TodoList_后续路线图与待办.md](./06_Roadmap_TodoList_后续路线图与待办.md)

## 5. 当前风险

- 文档状态容易落后于代码推进。
- persistence package 与真实持久化写入容易被混称。
- candidate table update audit 容易被误解为真实更新。
- 制度事实包仍是最小通电，不足以直接写涨跌停规则。
- 真实样本数量仍偏少。

## 6. 今日可交付判断

到 2026-07-01 为止，项目可以明确说：

- 结构资格主线已经立住。
- 首批真实 A 股样本已经通到制度审计与资格记录准备层。
- 第十四轮审计链路已完成。
- 第十五轮语义校准已完成。
- M1 candidate table update audit 已完成。
- M2 样本扩充与四类覆盖已完成。
- P3 制度研究准备层已完成收口。
- P4a staging 资格记录持久化验证已完成。
- P4b staging candidate table 更新验证已完成。
- P4c formal data root candidate table 写入入口已完成并通过临时 data root 验证。
- P5 trading layer readiness audit 已完成，formal candidate table 已准备进入未来 read gate contract review。
- 系统仍严格停在审计边界内。
- 未来未发生事项不再写入本文件的任务清单，改由路线图与待办文件承接。
