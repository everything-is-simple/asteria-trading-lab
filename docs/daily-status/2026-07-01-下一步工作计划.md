# 2026-07-01 下一步工作计划

## 一、当前确认状态

截至 2026-07-01 当前检查，仓库最新主线已经推进到：

`ready_for_institution_rule_definition_draft_review`

当前关键证据：

1. 当前分支为 `Asteria-malf-pas/institution-facts-minimal-power`。
2. 当前分支 `Asteria-malf-pas/institution-facts-minimal-power` 已与远端 `origin` 对齐。
3. P5/P6/P7 已完成 spec、plan、实现、导出与 focused 测试：
   - `docs/superpowers/specs/2026-07-01-p7-institution-rule-definition-readiness-design.md`
   - `docs/superpowers/plans/2026-07-01-p7-institution-rule-definition-readiness.md`
   - `src/data_sources/tdx_local/first_batch.py`
   - `src/data_sources/tdx_local/__init__.py`
   - `tests/test_tdx_local_first_batch.py`
4. P7 focused 测试口径为 `tests.test_tdx_local_first_batch` 通过。
5. 当前全量测试口径为 `256 tests OK`。

当前真实含义是：

- formal candidate table 已通过 trading layer readiness audit；
- P6 read gate contract audit 已通过全量测试；
- P7 制度规则定义准备审计已通过 focused 测试；
- T+1、涨跌停、停复牌只作为 `draft_input_only=True` 的规则草案输入；
- 但 trading layer read 尚未开放；
- institution rule definition、signal、backtest 仍全部关闭。

## 二、当前仍关闭的硬闸

以下硬闸仍必须保持 `false`：

- `institution_rule_definition_allowed`
- `trading_layer_read_allowed`
- `signal_generation_allowed`
- `backtest_execution_allowed`
- `candidate_table_update_allowed`
- `qualification_record_write_allowed`
- `formal_data_write_allowed`

当前阶段不得把 P5 pass 理解为：

- 可以交易；
- 可以生成 signal；
- 可以生成仓位；
- 可以读取 trading layer；
- 可以定义正式制度规则；
- 可以运行 backtest。

P5 pass 只表示：

`ready_for_trading_layer_read_gate_review`

P6 pass 只表示：

`ready_for_trading_layer_read_contract_review`

P7 pass 只表示：

`ready_for_institution_rule_definition_draft_review`

## 三、子代理探索结论

本轮已建立两个只读探索子代理：

1. 文档侧子代理确认：
   - 当前主线基线是 `candidate_table_trading_layer_readiness_audit_passed`。
   - P5 已完成实现、测试与路线图收口。
   - P6 spec 草案已经存在，当时状态仍是待评审。
   - 下一步需要区分 P6 “草案已创建 / 已评审通过 / 可写 implementation plan / 可实现”四个状态。

2. 代码侧子代理确认：
   - P5 实现落点仍在 `src/data_sources/tdx_local/first_batch.py`。
   - P6 若进入实现，最自然仍放在 P5 readiness audit 附近。
   - 测试入口应继续使用 `tests/test_tdx_local_first_batch.py`。
   - P6 pass 也应继续保持 `trading_layer_read_allowed=False`，除非未来另有独立 gate 明确开放。

因此，今天的主线不是重复写 P6 草案，也不是直接实现 P6，而是先完成 P6 spec review 与实施计划准入判断。

## 四、下一步主线判断

当前不建议直接进入 signal 或 backtest。

下一步最稳的主线是：

1. 评审并定版 P6 trading layer read gate / consumer contract spec。
2. 修正文档状态口径，让 P5 已完成、P6 spec 已评审、P6 implementation plan 已创建、P6 代码尚未实现四件事同时清楚。
3. P6 implementation plan 已创建后，下一阶段才能按 TDD 写失败测试。
4. 只有先看到失败测试后，才开始 P6 代码实现。

P6 implementation plan 已创建为：

`docs/superpowers/plans/2026-07-01-p6-trading-layer-read-gate-contract.md`

当前评审结论：P6 第一版仍保持 `trading_layer_read_allowed=False`，只推进到：

`ready_for_trading_layer_read_contract_review`

## 五、下一步工作拆解

### Step 1：P6 spec review 与状态定版

目标：

- 评审 `docs/superpowers/specs/2026-07-01-p6-trading-layer-read-gate-contract-design.md`。
- 确认 P6 第一版是否正式采用 `trading_layer_read_allowed=False`。
- 确认 P6 pass 只推进到 `ready_for_trading_layer_read_contract_review`。
- 确认 P6 读取来源、审计对象、阻断条件、输出契约都已足够可实现。

建议触碰文件：

- `docs/superpowers/specs/2026-07-01-p6-trading-layer-read-gate-contract-design.md`
- `docs/06_Roadmap_TodoList_后续路线图与待办.md`
- `docs/daily-status/2026-07-01-下一步工作计划.md`

### Step 2：P5/P6 文档口径精修

目标：

- P5 spec 不再停留在“待评审草案”的误导状态。
- Roadmap 明确区分：
  - P6 spec 草案已创建；
  - P6 spec 已评审定版；
  - P6 implementation plan 已创建；
  - P6 代码尚未实现。
- daily status 保持当前计划与已发生事实一致。

验证命令：

```powershell
git diff --check
```

### Step 3：P6 实施计划

只有在 P6 spec 评审通过后，才写实施计划。

建议新建：

`docs/superpowers/plans/2026-07-01-p6-trading-layer-read-gate-contract.md`

计划必须坚持 TDD：

- 先写 `pass` 测试；
- 再写 `blocked` 测试；
- 再写 forbidden field 测试；
- 再写 hard gate 保持关闭测试；
- 最后实现。

### Step 4：P6 TDD 实现候选位置

最可能触碰：

- `src/data_sources/tdx_local/first_batch.py`
- `src/data_sources/tdx_local/__init__.py`
- `tests/test_tdx_local_first_batch.py`

如果需要把 P5 与 A 股 execution policy research agenda 串成总状态视图，可能触碰：

- `src/ashare_intake_validator.py`
- `tests/test_ashare_intake_validator.py`

但第一版应尽量保持范围小，优先在 candidate table 后、trading layer 前保持只读 audit 边界。

实现前置条件：

- P6 spec review 已完成；
- P6 implementation plan 已创建；
- pass / blocked / forbidden field / hard gate 测试点已写清；
- 先看到失败测试，再写生产代码。

## 六、下一阶段测试重点

新增审计阶段至少覆盖：

1. `pass`
2. `blocked`
3. 禁用字段校验
4. downstream hard gates 仍为 `false`

推荐 focused 命令：

```powershell
$env:PYTHONPATH='src'; $env:PYTHONDONTWRITEBYTECODE='1'; python -B -m unittest tests.test_tdx_local_first_batch -v
```

如果触碰 `ashare_intake_validator.py`，追加：

```powershell
$env:PYTHONPATH='src'; $env:PYTHONDONTWRITEBYTECODE='1'; python -B -m unittest tests.test_ashare_intake_validator -v
```

最终全量验证：

```powershell
$env:PYTHONPATH='src'; $env:PYTHONDONTWRITEBYTECODE='1'; python -B -m unittest discover -s tests -v
git diff --check
```

## 七、风险控制

下一阶段必须避免以下跑偏：

1. 把 P5 pass 解释成 trading layer 已开放。
2. 把 trading layer read gate 写成 signal generator。
3. 把 execution facts 写成 institution rules。
4. 把 T+1、涨跌停、停牌结论回流为：
   - `rhythm_meaning`
   - `tachibana_applicability`
   - `structure_suitable`
   - MALF 背景判断
5. 输出以下 forbidden fields：
   - `buy_signal`
   - `sell_signal`
   - `trade_accept`
   - `trade_reject`
   - `trade_defer`
   - `signal_decision`
   - `target_position`
   - `position_size`
   - `ashare_t1_action`
   - `limit_up_strategy`
   - `limit_down_strategy`
   - `industry_hot_score`
   - `liquidity_rank_as_applicability`
   - `rhythm_meaning_override`
   - `tachibana_applicability_override`

## 八、外部数据根约束

真实数据根仍在仓库外：

- `Z:\asteria-trading-labs-data`
- `Z:\malf-data`
- `Z:\new_tdx64`
- `Z:\tdx_offline_Data`

默认约束：

- 不复制 DuckDB 入库；
- 不复制通达信原始二进制入库；
- 不提交真实市场导出数据；
- 不写 `Z:\asteria-trading-labs-data`，除非命令明确是受控导出/写入；
- 所有测试继续使用临时目录和 fixtures。

## 九、建议今日下一动作

P7 主线已经按 TDD 完成实现与全量验证，下一动作是提交推送：

1. 已跑全量测试：
   `$env:PYTHONPATH='src'; $env:PYTHONDONTWRITEBYTECODE='1'; python -B -m unittest discover -s tests -v`
2. 跑 diff whitespace 检查：
   `git diff --check`
3. 若全绿，提交 P7 里程碑。
4. 推送当前分支。

一句话收口：

> 当前系统已完成 P7 制度规则定义准备审计；下一步只是在全量验证通过后提交推送，仍继续守住 institution rule、trading layer read、signal、backtest 硬闸。

## 十、执行进展

截至本轮继续施工，已按本计划完成：

1. 更新 `docs/04_施工计划_当前进度版.md`，将当前系统位置收口为 `candidate_table_trading_layer_readiness_audit_passed`。
2. 更新 `docs/06_Roadmap_TodoList_后续路线图与待办.md`，将 P5 标记为已完成的 trading layer readiness audit，并将 P6 调整为 trading layer read gate / consumer contract。
3. 更新 `docs/daily-status/README.md`，补齐 2026-06-30 与 2026-07-01 的 daily status 索引。
4. 新增 `docs/superpowers/specs/2026-07-01-p6-trading-layer-read-gate-contract-design.md`，完成 P6 前置规格草案。
5. 完成 P6 spec review，将 P6 spec 状态定为“已评审定版，可写 implementation plan；尚未实现”。
6. 精修 P5/P6 文档状态，明确 P5 spec 已实现归档，P6 roadmap 只完成规格设计，尚未实现。
7. 新建 `docs/superpowers/plans/2026-07-01-p6-trading-layer-read-gate-contract.md`，要求先写 pass / blocked / forbidden field / hard gate / no trading layer read opened 测试。
8. 按 P6 implementation plan 增加 RED 测试，确认新入口未导出时测试失败。
9. 实现 `audit_trading_layer_read_gate_contract_when_explicitly_requested` 并从 `src/data_sources/tdx_local/__init__.py` 导出。
10. focused 验证通过：
    `$env:PYTHONPATH='src'; $env:PYTHONDONTWRITEBYTECODE='1'; python -B -m unittest tests.test_tdx_local_first_batch -v`
11. 全量验证通过：
    `$env:PYTHONPATH='src'; $env:PYTHONDONTWRITEBYTECODE='1'; python -B -m unittest discover -s tests -v`
12. 新增 P7 spec：
    `docs/superpowers/specs/2026-07-01-p7-institution-rule-definition-readiness-design.md`
13. 新增 P7 implementation plan：
    `docs/superpowers/plans/2026-07-01-p7-institution-rule-definition-readiness.md`
14. 按 P7 implementation plan 增加 RED 测试，确认新入口未导出时测试失败。
15. 实现 `audit_institution_rule_definition_readiness_when_explicitly_requested` 并从 `src/data_sources/tdx_local/__init__.py` 导出。
16. focused 验证通过：
    `$env:PYTHONPATH='src'; $env:PYTHONDONTWRITEBYTECODE='1'; python -B -m unittest tests.test_tdx_local_first_batch -v`
17. 全量验证通过：
    `$env:PYTHONPATH='src'; $env:PYTHONDONTWRITEBYTECODE='1'; python -B -m unittest discover -s tests -v`

当前已完成 P7 实现与全量验证；P7 pass 只表示 `ready_for_institution_rule_definition_draft_review`，不开放正式 `institution_rule_definition_allowed`，也不开放真实 `trading_layer_read_allowed`。

## 十一、今日完成定义

今天优先交付的不是 trading signal，也不是 backtest，而是一个清楚可执行的 P7 制度规则定义准备状态：

1. P7 spec 已明确 T+1、涨跌停、停复牌只作为规则草案输入。
2. P7 implementation plan 已完成。
3. P7 已按 TDD 覆盖 pass / blocked / forbidden field / hard gates false。
4. P7 focused 与全量验证已通过。
5. 所有硬闸继续保持关闭：
   - `institution_rule_definition_allowed=False`
   - `trading_layer_read_allowed=False`
   - `signal_generation_allowed=False`
   - `backtest_execution_allowed=False`
